version: '3.8'

services:
  ascend-web:
    build: .
    container_name: ascend_simulation
    ports:
      - "8080:80"
    volumes:
      # specific mapping to ensure patient data created in the app is saved to your computer
      - ./patients:/var/www/html/patients
    environment:
      # The API Key extracted from your chat.php file
      - GEMINI_API_KEY=AIzaSyCmJvnap11eKE9Iob6TUQvcVpmj20Uq-pM
    restart: unless-stopped